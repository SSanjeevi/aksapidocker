apiVersion: v1
kind: Service
metadata:
  name: {{ template "aksdockerapi.fullname" . }}
  labels:
    app: {{ template "aksdockerapi.name" . }}
    chart: {{ template "aksdockerapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "aksdockerapi.name" . }}
    release: {{ .Release.Name }}
